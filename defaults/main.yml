# Standards: 1.2
---
# @var nvidia_version:description: Version of Nvidia graphics driver
nvidia_version: "530"

# @var nvidia_packages:description: List of packages to install
nvidia_packages:
  - "nvidia-graphics-driver-{{ nvidia_version }}"
  - "libvulkan1"
  - "libvulkan1:i386"

# @var nvidia_enable_i386:description: Enable installation of i386 packages
nvidia_enable_i386: True

...
